// Beginner Level

// Focus: Basic if / else logic, comparison operators, and syntax understanding.

// Write a program that checks if a number is positive, negative, or zero.

// let num = prompt("Enter a number");
// if (num > 0) {
//   console.log("given number is positive");
// } else if (num < 0) {
//   console.log("given number is negative");
// } else {
//   console.log("given number is zero");
// }
// Check if a person is eligible to vote (age ≥ 18).
// let age = prompt("enter your age to confirm");
// if (age >= 18) {
//   console.log("yes you are eligible to voting");
// } else if (age < 18) {
//   console.log(" Not eligible");
// }

// Given a number, check if it’s even or odd.
// let numcheck = Number(prompt("Enter a number to check"));
// if (numcheck % 2 === 0) {
//   console.log("yes it is even number");
// } else {
//   console.log("odd number ");
// }

// If a variable score is greater than or equal to 50, print "Pass", else print "Fail".
// let variable = prompt("Enter a score");
// if (variable >= 50) {
//   console.log("pass");
// } else {
//   console.log("failed");
// }
// Write a program to check whether a character is a vowel or consonant.
let vowels = ["a", ""];

// Given two numbers, print the larger one.

// Check whether a given year is a leap year or not.

// Write a program that checks if a given number is divisible by 5 and 11.

// 🟡 Intermediate Level

// Focus: Multiple conditions, nested if, logical operators (&&, ||), and ternary usage.

// Given three numbers, find the largest among them.

// Check if a given character is an alphabet, digit, or special character.

// A shop gives a discount of 10% if the purchase amount is above 1000.

// If not, no discount.

// Calculate the total amount to pay.

// Check if a year is a century leap year (e.g., 2000 is, 1900 is not).

// Write a program that checks whether a person is a child (0–12), teen (13–19), adult (20–59), or senior (60+).

// Using a ternary operator, print “Even” if a number is even, otherwise “Odd”.

// Check if a number is within the range of 10 to 50 (inclusive).

// Given marks in three subjects, determine if the student passed (each subject ≥ 40) and if they qualified for distinction (average ≥ 75).

// Write a program that takes a time in 24-hour format and prints "Good Morning", "Good Afternoon", "Good Evening", or "Good Night".

// 🔵 Advanced Level

// Focus: switch statements, nested conditions, combined logic, and small projects.

// Use a switch statement to print the day of the week based on a number (1 = Monday, ..., 7 = Sunday).

// Use a switch to perform a basic calculator that accepts two numbers and an operator (+, -, *, /).

// Given a month number, print how many days that month has.

// Check if a triangle is valid based on its three angles (sum must be 180).

// Given the sides of a triangle, classify it as equilateral, isosceles, or scalene.

// Write a program that checks if a character is uppercase, lowercase, digit, or special symbol (use ASCII ranges).

// Implement a grading system:

// 90–100 → A

// 80–89 → B

// 70–79 → C

// 60–69 → D

// <60 → F

// Using nested ifs, check if a number is:

// Positive and even

// Positive and odd

// Negative and even

// Negative and odd

// Build a small login simulation:

// If username = "admin" and password = "1234", print "Login Successful".

// Else print "Invalid Credentials".

// Advanced switch challenge:

// Create a program that takes an operation code (like "sum", "avg", "max", "min") and performs that operation on an array of numbers.

// 🔴 Expert-Level Challenges

// Combine conditions, logical operators, and problem-solving.

// Write a program to determine if a given year, month, and day form a valid date.

// Simulate a traffic light system:

// If "red" → "Stop",

// "yellow" → "Ready",

// "green" → "Go",

// otherwise → "Invalid light".

// Create a menu-based system using switch:

// 1 → Check even/odd

// 2 → Check leap year

// 3 → Check positive/negative

// 4 → Exit

// Given a temperature value, print its category (freezing, cold, warm, hot, very hot).

// Build a mini quiz program:

// Ask 3 questions.

// Use conditionals to calculate and display the score.

// Given a number, check whether it is a prime number using if-else logic.

// Using nested ifs, determine if three sides form a right triangle (Pythagorean theorem).

// Write a small ATM simulation:

// Ask for PIN.

// If correct, show options: balance, withdraw, deposit, exit.

// Validate conditions properly using nested conditionals and switch.
