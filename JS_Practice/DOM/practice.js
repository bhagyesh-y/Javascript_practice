// Beginner Level

// Select a <p> tag by its ID and change its text content.
let change = document.getElementById("main");
change.innerText = "this is new content in the page";

// Change the background color of a <div> when a button is clicked.

let button = document.getElementById("changeBtn");
let box = document.getElementById("myBox");

button.addEventListener("click", () => {
  box.style.backgroundColor = "lightblue";
});

// Add a new <li> item to an existing unordered list (<ul>) using JavaScript.

// Remove the last <li> from a list when a button is pressed.

// Toggle the visibility of an element (show/hide) with a button click.

// 🔹 Intermediate Level

// Create a simple form and display the entered input values inside a <div> without refreshing the page.

// Build a counter with + and - buttons that increases/decreases the number displayed.

// When hovering over an image, change it to another image, and switch back on mouse out.

// Append a new paragraph to the body every time a button is clicked.

// Highlight all paragraphs (<p>) on the page by changing their text color with one button.

// 🔹 Advanced Level

// Create a to-do list: add, mark as done, and delete items dynamically.

// Build a light/dark mode toggle by switching CSS classes with JS.

// Create a dropdown menu that shows/hides options when clicked.

// Implement an image slider (next/prev buttons to switch images).

// Create a table dynamically from an array of objects (e.g., name, age, email).

// Add drag-and-drop functionality to move items between two lists.

// Build a simple calculator (buttons for numbers and operators, display result).

// Capture keyboard events (keydown) to move a square <div> around the screen.

// Create a live search bar that filters a list of names as the user types.

// Implement a modal popup that opens on button click and closes when clicking outside or pressing Esc.
